// The page's typographic climax lives on the marketing layer only: every section header shares one
// confident scale so hierarchy reads as deliberate, not drifting. Fixed clamp steps are sanctioned
// here (not in app UI) per DESIGN.md. The hero's larger display scale is local to Hero.tsx.

/** Shared section-heading scale for the marketing home page. */
export const sectionHeading =
    'text-[2rem] font-bold leading-[1.08] tracking-[-0.025em] text-foreground [text-wrap:balance] sm:text-4xl lg:text-[2.75rem]';
