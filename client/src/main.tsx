import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";

// Testing if I can get this commit to main in replit

createRoot(document.getElementById("root")!).render(<App />);
