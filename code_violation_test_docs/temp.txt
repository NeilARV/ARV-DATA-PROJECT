Okay take a look at @.claude/plans/code-violation.md . I want you to create a test csv that has mock properties to test the processing of the CSV and informing stuff. We also need to make sure we have mock properties where my email (justin@arvfinance.com) is associated with a random company and that company is the most recent transaction. Overall we just need a whole mock file and mock properties and things across our application. Any one off  files or things that we are using to testing like the .csv file can be put into @code_violation_test_docs folder. We can create different CSVs with different scenarios, but we just need these things to be able to test locally and to be able to manually confirm things are working as expected. We may have already Added mock properties and stuff to help account for this, but I am unsure. Please check and veryify. if not then we need to add them ourselves. I created a second branch on the code violation feature branch to work on this so we can test then just delete al the extra test stuff easily. 