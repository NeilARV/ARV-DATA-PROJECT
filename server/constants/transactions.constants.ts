// Lender name (as SFR Analytics returns it, upper-cased) that marks a transaction as ARV-funded.
// Compare against `firstMtgLenderName?.trim().toUpperCase()`.
export const ARV_LENDER = 'ARV FINANCE INC';
